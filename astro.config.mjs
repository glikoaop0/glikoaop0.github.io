import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://yourusername.github.io',
  // If deploying to a sub-path (e.g. github.io/repo-name), set base:
  // base: '/repo-name',
});
